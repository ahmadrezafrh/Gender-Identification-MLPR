This folder contains the main algorithms that we want to test
over a set of hyperparameters (trained on train.txt evaluated on test.txt).
Each file requires a mandatory configue files. All the configues should created in
'./configues/test' directory.

e.g. "~$pyhton3 gmm.py part6_1_gmm.jason"